/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      's13emagst.akamaized.net', // emag
      'lcdn.altex.ro',           // altex
      'lcdn.mediagalaxy.ro',     // mediagalaxy
      's1.cel.ro',               // cel
      'static.nike.com',         // nike
      
    ],
  },
};

export default nextConfig;
